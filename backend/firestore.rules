rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for development (emulator only)
    // In production, replace with proper security rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
