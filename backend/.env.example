# =============================================================================
# REQUIRED CONFIGURATION (Edit these)
# =============================================================================

# LLM Provider (choose one: openai, anthropic)
LLM_PROVIDER=openai
OPENAI_API_KEY=your-openai-key-here
# ANTHROPIC_API_KEY=your-anthropic-key-here  # Uncomment if using Anthropic

# =============================================================================
# OPTIONAL CONFIGURATION (Defaults are fine for development)
# =============================================================================

# Application
PROJECT_NAME=Enterprise App
VERSION=1.0.0
ENVIRONMENT=development
DEBUG=true
API_V1_PREFIX=/api/v1

# Firebase
FIREBASE_PROJECT_ID=demo-project
FIREBASE_CREDENTIALS_PATH=
USE_FIREBASE_EMULATOR=true
FIREBASE_EMULATOR_HOST=localhost
FIREBASE_EMULATOR_PORT=8080

# Security (auto-generated by setup.sh if empty)
SECRET_KEY=
JWT_SECRET=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# =============================================================================
# ADVANCED CONFIGURATION (Don't change unless needed)
# =============================================================================

# CORS (frontend URLs - comma separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:19006

# LLM Settings
LLM_MODEL=gpt-4-turbo-preview
ENABLE_RAG=false
VECTOR_STORE_TYPE=inmemory
CHROMA_HOST=localhost
CHROMA_PORT=8000

# Redis (only needed for background tasks with Celery)
# REDIS_URL=redis://localhost:6379/0
# CELERY_BROKER_URL=redis://localhost:6379/1
# CELERY_RESULT_BACKEND=redis://localhost:6379/2

# Email (optional - for production)
# EMAIL_ENABLED=false
# SENDGRID_API_KEY=
# FROM_EMAIL=noreply@example.com

# Monitoring (optional - for production)
# SENTRY_DSN=
